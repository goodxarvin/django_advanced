services:
  backend:
    build: .
    container_name: backend
    environment:
      - SECRET_KEY=test
      - DEBUG=True
    ports:
      - "8001:8000"
    volumes:
      - .:/app
    command: python manage.py runserver 0.0.0.0:8000
    
